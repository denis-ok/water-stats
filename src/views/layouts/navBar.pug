nav.navbar.navbar-expand-lg.navbar-light.bg-light.border-bottom.p-3.px-md-4
  a.navbar-brand(href='/') Water Stats
  button.navbar-toggler(type='button' data-toggle='collapse' data-target='#navbarSupportedContent')
    span.navbar-toggler-icon
  #navbarSupportedContent.collapse.navbar-collapse
    ul.navbar-nav.mr-auto


    ul.navbar-nav
      if isUserSignedIn()
        li.nav-item.active.px-2
          a.btn.btn-primary(href='#') Add Meter Readout
        li.nav-item.active.px-2
          a.btn.btn-secondary(href='#') Manage Water Meters
        li.nav-item.dropdown
          a#navbarDropdown.nav-link.dropdown-toggle(href='#' role='button' data-toggle='dropdown')
            | Profile
          .dropdown-menu.dropdown-menu-right(aria-labelledby='navbarDropdown')
            a.dropdown-item(href=urlFor('userProfile', getUserId())) View Profile
            a.dropdown-item(href=urlFor('editUser', getUserId())) Edit Profile
            .dropdown-divider
            a.dropdown-item(href=urlFor('sessionDelete') data-method='delete' rel='nofollow') Sign Out
      else
        li.nav-item.px-2
          a.nav-link(href=urlFor('newSession')) Sign In


